import 'package:flutter/material.dart';

class AppColors {
  // Prevent instantiation
  AppColors._();

  /// App Background: Off-white Zinc-50
  static const Color background = Color(0xFFFAFAFA);

  /// Surface / Card background: Pure white
  static const Color surface = Color(0xFFFFFFFF);

  /// Primary Text: Dark Gray Zinc-800
  static const Color textPrimary = Color(0xFF27272A);

  /// Secondary Text: Muted Gray Zinc-500
  static const Color textSecondary = Color(0xFF71717A);

  /// Muted / Hint Text: Zinc-400
  static const Color textMuted = Color(0xFFA1A1AA);

  /// Primary Accent: Vibrant Emerald Green
  static const Color primary = Color(0xFF10B981);
  static const Color primaryLight = Color(0xFFD1FAE5);

  /// Warning / Destructive: Red
  static const Color destructive = Color(0xFFEF4444);
  static const Color destructiveLight = Color(0xFFFEE2E2);

  /// Border / Divider: Zinc-200
  static const Color border = Color(0xFFE4E4E7);
  static const Color borderSubtle = Color(0xFFF4F4F5);

  /// Thermal ticket background
  static const Color thermalTicketBg = Color(0xFFFFFDF7);
  static const Color thermalInk = Color(0xFF18181B);
}
